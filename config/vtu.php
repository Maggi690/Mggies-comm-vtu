<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Primary VTU Provider
    |--------------------------------------------------------------------------
    */
    'primary_provider' => env('VTU_PRIMARY_PROVIDER', 'vtpass'),

    /*
    |--------------------------------------------------------------------------
    | Exam Prices (NGN)
    |--------------------------------------------------------------------------
    */
    'exam_prices' => [
        'waec'   => (int) env('WAEC_PRICE', 4000),
        'neco'   => (int) env('NECO_PRICE', 1500),
        'nabteb' => (int) env('NABTEB_PRICE', 900),
        'jamb'   => (int) env('JAMB_PRICE', 3500),
    ],

    /*
    |--------------------------------------------------------------------------
    | Supported Networks
    |--------------------------------------------------------------------------
    */
    'networks' => ['mtn', 'airtel', 'glo', '9mobile'],

    /*
    |--------------------------------------------------------------------------
    | Service Limits
    |--------------------------------------------------------------------------
    */
    'limits' => [
        'airtime' => [
            'min' => 50,
            'max' => 50000,
        ],
        'data' => [
            'min' => 100,
            'max' => 100000,
        ],
        'electricity' => [
            'min' => 100,
            'max' => 500000,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Provider Retry Settings
    |--------------------------------------------------------------------------
    */
    'max_retries'      => 3,
    'failure_backoff'  => 600,  // seconds
    'max_failures'     => 5,    // before backoff

    /*
    |--------------------------------------------------------------------------
    | Referral Commission
    |--------------------------------------------------------------------------
    */
    'referral' => [
        'signup_bonus'      => (float) env('REFERRAL_SIGNUP_BONUS', 100),
        'transaction_rate'  => (float) env('REFERRAL_TRANSACTION_RATE', 0.5), // % per transaction
        'enabled'           => env('REFERRAL_ENABLED', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | Cache TTLs (seconds)
    |--------------------------------------------------------------------------
    */
    'cache' => [
        'providers'  => 300,
        'data_plans' => 3600,
        'settings'   => 600,
    ],
];
